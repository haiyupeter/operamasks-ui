project.name=operamasks-ui
release.version=2.1
jsdoc.builder.dir=lib/jsdoc-lib
jsdoc.lib.dir=${jsdoc.builder.dir}/jsdoc-toolkit
docs.dir=../docs
release.dir=../release
build.dir=../../build
build.project=${build.dir}/${project.name}
# classes.dir=../WEB-INF/classes
release.development.dir=${release.dir}/${project.name}/development-bundle
themes=default,apusic,elegant